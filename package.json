{
  "name": "release-it-demo",
  "version": "3.1.4",
  "scripts": {
    "release-setup": "release-it --github.release=false && echo \"Pull Request the release changes back to main branch to initiate the release process\"",
    "release-process-ci": "dotenv release-it --github.release=true --github.preRelease=true --no-git --no-plugins.@release-it/conventional-changelog.infile --no-increment --ci",
    "release-finalise-ci": "dotenv release-it --github.release=true --github.preRelease=false --no-git --no-plugins.@release-it/conventional-changelog.infile --no-increment --ci"
  },
  "devDependencies": {
    "@release-it/conventional-changelog": "^10.0.1",
    "dotenv-cli": "^10.0.0",
    "release-it": "^19.0.4"
  }
}
