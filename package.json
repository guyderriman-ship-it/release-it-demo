{
  "name": "release-it-demo",
  "version": "3.1.8",
  "scripts": {
    "release-initiate": "release-it --github.release=false && echo \"Pull Request the release changes back to main branch to initiate the release process\"",
    "release-publish-prerelease-ci": "dotenv -- release-it --no-increment --ci --verbose --no-git --github.release --github.update --github.preRelease=true --no-plugins.@release-it/conventional-changelog.infile",
    "release-finalise-prerelease-ci": "dotenv -- release-it --no-increment --ci --verbose --no-git --github.release --github.update --github.preRelease=false --no-plugins.@release-it/conventional-changelog.infile"
  },
  "devDependencies": {
    "@release-it/conventional-changelog": "^10.0.1",
    "dotenv-cli": "^10.0.0",
    "release-it": "^19.0.4"
  }
}
